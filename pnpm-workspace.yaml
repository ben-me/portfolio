shellEmulator: true

trustPolicy: no-downgrade

trustPolicyExclude:
  - chokidar
  - semver

overrides:
  nuxt>shell-quote: ^1.8.4
  vite: ^7.3.5

onlyBuiltDependencies:
  - "@parcel/watcher"
  - esbuild
allowBuilds:
  "@parcel/watcher": true
  esbuild: true
  sharp: true
